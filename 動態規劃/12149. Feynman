#include<bits/stdc++.h>
using namespace std;
int main(){
	int num[105],n;
	memset(num,0,sizeof(num));
	for(int i=1;i<=100;i++){
		num[i]=i*i+num[i-1];
	} 
	while(cin>>n){
		if(n==0)break;
		cout<<num[n]<<"\n";
	}
}
